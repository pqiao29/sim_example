1. Change parameters in get_sbatch.sh: 
    range of RDR variance (RDR_var), K (K_range), number of cores (ncores), number of simulations (nsims)

2. source get_sbatch.sh to get sbatch.sh

3. mkdir log
   mkdir result

4. sh sbatch.sh

5. source plot_result.R => States.png, Clusters.png

